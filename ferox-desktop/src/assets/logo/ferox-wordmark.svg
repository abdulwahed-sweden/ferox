<svg viewBox="0 0 280 100" xmlns="http://www.w3.org/2000/svg">
  <!-- Ferox Wordmark - Fox Logo + FEROX Text -->

  <!-- Fox Head Icon -->
  <g transform="translate(0, 0)">
    <!-- Main fox head outline -->
    <path
      d="M50 5
         L20 25
         L15 20
         L10 35
         L5 55
         L15 70
         L25 80
         L35 88
         L50 95
         L65 88
         L75 80
         L85 70
         L95 55
         L90 35
         L85 20
         L80 25
         L50 5
         Z"
      fill="currentColor"
    />

    <!-- Left inner ear (negative space) -->
    <path
      d="M25 28 L22 38 L28 45 L32 35 L25 28 Z"
      fill="var(--logo-bg, #12161F)"
    />

    <!-- Right inner ear (negative space) -->
    <path
      d="M75 28 L78 38 L72 45 L68 35 L75 28 Z"
      fill="var(--logo-bg, #12161F)"
    />

    <!-- Left eye (negative space) -->
    <path
      d="M30 50 L25 55 L30 60 L38 55 L30 50 Z"
      fill="var(--logo-bg, #12161F)"
    />

    <!-- Right eye (negative space) -->
    <path
      d="M70 50 L75 55 L70 60 L62 55 L70 50 Z"
      fill="var(--logo-bg, #12161F)"
    />

    <!-- Left cheek mark (negative space) -->
    <path
      d="M18 58 L22 65 L28 62 L24 55 L18 58 Z"
      fill="var(--logo-bg, #12161F)"
    />

    <!-- Right cheek mark (negative space) -->
    <path
      d="M82 58 L78 65 L72 62 L76 55 L82 58 Z"
      fill="var(--logo-bg, #12161F)"
    />
  </g>

  <!-- FEROX Text -->
  <text
    x="110"
    y="68"
    font-family="Inter, system-ui, -apple-system, sans-serif"
    font-size="56"
    font-weight="700"
    fill="currentColor"
    letter-spacing="-1"
  >FEROX</text>
</svg>
